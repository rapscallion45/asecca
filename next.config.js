/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    APP_NAME: `Asecca`,
    APP_DESCRIPTION: `Asecca front end client SPA`,
  },
};

module.exports = nextConfig;
